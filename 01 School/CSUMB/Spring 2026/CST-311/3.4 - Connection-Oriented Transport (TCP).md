---
📕 Courses:
  - "!!Intro to Computer Networks"
tags:
  - ComputerScience/Networks
Date Created: 2026-03-02
---
```table-of-contents
```
---
# Transmission Control Protocol (TCP)
- **Reliable** transport between sending and receiving process
- **Flow control**: Sender won't overwhelm receiver
- **Congestion control**: Throttle sender when network overloaded
- Does not provide
	- Timing
	- Minimum throughput guarantee
	- Security
- Connection-Oriented: Setup required between client and server processes

# TCP Segment Structure
![[Pasted image 20260302143336.png|540]]

## Connection-Oriented Protocol
- Why do we need connection setup?
	- To establish **state** on both hosts
	- Most important state: sequence numbers
		- Count the number of bytes that have been sent
		- Initial value chosen at random. Why?
			- Security reasons. This practice helps mitigate the risk of certain types of attacks such as sequence number prediction attacks and connection hijacking

## Sequence Number
![[Pasted image 20260302143546.png|450]]

# Important TCP Flags
- TCP (Transmission Control Protocol) uses a set of flags within its header to control and manage various aspects of the communication  between two devices
- There are 6 control flags
- Important Flags
	- **SYN**: synchronization, used for connection setup
	- **ACK**: acknowledge received data
	- **FIN**: finish, used to tear down connection
## TCP Flags
![[Pasted image 20260302143817.png|450]]
## Three Way Handshake
![[Pasted image 20260302144013.png|350]]
![[Pasted image 20260302144034.png|350]]
- Each Side:
	- Notifies the other of starting sequence numbers
	- ACKs the other side's starting sequence number
![[Pasted image 20260302144250.png|350]]

# Analyzing TCP Connections with Wireshark
- Wireshark uses **relative sequence numbers**
	- The client and server start with sequence number = 0
	- We can retrieve the raw sequence number from the packet info
- Next sequence number
	- The next expected sequence number/ next expected ack. number
	- EX: if the current sequence number is 1, and 214 bytes of TCP payload were sent , then the next expected sequence number should be 215

# Sequence Number Space
- TCP uses a byte stream abstraction
	- Each byte in each stream is numbered
	- 32-bit value, wraps around
	- Initial, random values selected during setup
- Byte stream broken down into segments (packets)
	- Size limited by the Maximum Segment Size (MSS)
	- Set to limit fragmentation
- Each segment has a sequence numbers
![[Pasted image 20260302144741.png|400]]
## Exchange Packets
- After establishing connection
![[Pasted image 20260302145037.png|300]]

# Bidirectional Communication
- After establishing connection
![[Pasted image 20260302145115.png|350]]
- Each side of the connection can send and receive
	- Different sequence numbers for each direction
## Connection Tear Down Slides
- Either side can initiate tear down
- Other side may continue sending data
![[Pasted image 20260302145345.png|300]]

# Maximum Segment Size
- Byte stream broken down into segments (packets)
	- Size limited by the Maximum Segment Size (MSS)
	- Set to limit fragment
- Measures the non-header portion of a packet, which is called the payload
- MSS is determined by the maximum transmission (MTU)
	- MSS = MTU - (TCP header + IP header)
![[Pasted image 20260302145835.png|300]]

# Retransmission
- Packets could be lost or damaged
	- This leads to a lack of acknowledgment
	- The sender will resend the packets
- When to resend?
	- Use a timer when sending a packet
	- Lack of ACK will trigger the timer to timeout
		- Retransmit the packet when the time expires
## Retransmission Scenario 1
- When the data packet is lost or erroneous
![[Pasted image 20260302150220.png|350]]
## Retransmission Scenario 2
- When the ACK is lost/damaged
![[Pasted image 20260302150325.png|350]]
- Duplicate Packet: There are two copies of the packets at the receiver
## Retransmission Scenario 3
- When the ACK is delayed
![[Pasted image 20260302150430.png|350]]
## How Long Should the Sender Wait?
- Scenarios 2 and 3 could be avoided to improve efficiency
	- Set a proper timer. How?
- **Too Short**: Premature timeout, unnecessary retransmissions
- **Too long**: Slow reaction to segment loss: Excessive delay when the packet is lost
- TCP sets the timeout as a function of the RTT (round trip time)
	- **RTT**: The time required for the packet to travel from the source to the destination and then come back again
	- RTT varies depending on the connection
### How To Estimate RTT?
- `SampleRTT`: Measured time from segment transmission until ACK receipt
- `SampleRTT` will vary, want estimated RTT "smoother"
	- Average several recent measurements, not just current `SampleRTT`
- Problem: Timeout period can be relatively long or too short
	- When a segment is lost, this long timeout period forces the sender to delay resending the lost packet
## Problem with RTT-Based Strategy
![[Pasted image 20260302151416.png|350]]
- There is an ambiguity of not knowing whether the acknowledgment is for the original transmission or for the retransmission
- Whenever the retransmission occurs, it starts sending from the lost packet onward
	- This is inefficient
![[Pasted image 20260302151538.png|350]]

# TCP Receiver: ACK Generation
![[Pasted image 20260302151650.png|400]]

# Fast Retransmission
- TCP fast retransmit
	- If sender receives 3 additional ACKs for same data ("triple duplicate ACKs"), resend unacknowledged segment with smallest seq #
		- Likely that unacknowledged segment lost, so don't wait for timeout
![[Pasted image 20260302152449.png|300]]
- Receipt of three duplicate ACKs indicates 3 segments received after a missing segment - lost segment is likely, so retransmit
## What Will Happen After the Retransmission?
---
# References
1. 